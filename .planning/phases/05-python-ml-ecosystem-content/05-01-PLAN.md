---
phase: 05-python-ml-ecosystem-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/python-ml/index.md
  - docs/python-ml/numpy.md
autonomous: true
requirements: [PYML-01]

must_haves:
  truths:
    - "NumPy page covers arrays, indexing, vectorization, broadcasting, linear algebra ops, and random/statistics with ML motivation"
    - "Every sub-topic has runnable Python code examples with syntax highlighting and line numbers"
    - "Section index page has prerequisite callout, topic overview table, recommended order, and Why This Path admonition"
  artifacts:
    - path: "docs/python-ml/numpy.md"
      provides: "Complete NumPy content page with 6 sub-topics"
      min_lines: 180
    - path: "docs/python-ml/index.md"
      provides: "Upgraded section index with overview table and learning path"
      min_lines: 25
  key_links:
    - from: "docs/python-ml/index.md"
      to: "docs/python-ml/numpy.md"
      via: "relative link in topic table"
      pattern: "\\[NumPy\\]\\(numpy\\.md\\)"
    - from: "docs/python-ml/numpy.md"
      to: "docs/python-ml/pandas.md"
      via: "Next up link at page bottom"
      pattern: "\\*\\*Next up:\\*\\*.*pandas"
---

<objective>
Write the complete NumPy content page and upgrade the Python ML section index page.

Purpose: PYML-01 requires a NumPy section covering array operations, broadcasting, and vectorized computation. The index page needs upgrading from placeholder to full section overview (matching the math-foundations/index.md pattern).
Output: Two files -- docs/python-ml/numpy.md (full content) and docs/python-ml/index.md (upgraded index)
</objective>

<execution_context>
@/home/a-khouja/.claude/get-shit-done/workflows/execute-plan.md
@/home/a-khouja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTENT-CHECKLIST.md
@.planning/phases/05-python-ml-ecosystem-content/05-RESEARCH.md

Reference files to follow:
@docs/core-ml/supervised-learning.md (content template -- match this structure)
@docs/math-foundations/index.md (index page template -- match this structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete NumPy content page</name>
  <files>docs/python-ml/numpy.md</files>
  <action>
Replace the placeholder numpy.md with a complete content page following the supervised-learning.md template. This is a CODE-HEAVY page (not equation-heavy like Phase 4).

Structure (6 sub-topics from research):

1. **Page header:** H1 "NumPy", prerequisite admonition linking to `../math-foundations/index.md` (Math Foundations section) and noting basic Python assumed, metadata line `*Total time: ~6-8 hours* | :green_circle: Beginner`, Learning Outcomes (4-5 bullets).

2. **Sub-topic 1: Arrays and Array Creation** (~45 min) -- `np.array()`, `np.zeros()`, `np.ones()`, `np.arange()`, `np.linspace()`, dtype, shape. ML connection: every dataset is an ndarray. Code example: create arrays of different shapes, print shape/dtype.

3. **Sub-topic 2: Indexing, Slicing, and Reshaping** (~1 hr) -- slicing syntax, fancy indexing, boolean indexing, `reshape()`, `flatten()`. ML connection: selecting features, batching data, reshaping for model input. Code example: load a small dataset (use `np.random.seed` for reproducibility), demonstrate boolean masking to filter rows.

4. **Sub-topic 3: Array Operations and Vectorization** (~1.5 hr) -- element-wise ops, ufuncs, axis parameter, performance vs loops. ML connection: vectorized ops replace slow Python loops. Code example: compare loop vs vectorized computation showing why vectorization matters.

5. **Sub-topic 4: Broadcasting** (~1 hr) -- broadcasting rules, shape compatibility, common patterns. ML connection: feature scaling (subtract mean, divide by std), adding bias terms. Code example: standardize a feature matrix using broadcasting. Include LaTeX for the standardization formula.

6. **Sub-topic 5: Linear Algebra with NumPy** (~1 hr) -- `np.dot()`, `@` operator, `np.linalg.inv()`, `np.linalg.eig()`, `np.linalg.svd()`. ML connection: matrix operations for ML. Code example: compute dot product and matrix multiply, show `@` operator preference. Link back to `../math-foundations/linear-algebra.md`.

7. **Sub-topic 6: Random Number Generation and Statistics** (~45 min) -- `np.random`, `np.mean()`, `np.std()`, `np.percentile()`, seed for reproducibility. ML connection: sampling, weight initialization, computing statistics. Code example: generate random data, compute summary stats, demonstrate seed.

8. **Page footer:** Key Takeaways (4-5 bold-phrase bullets), Next up link to `pandas.md`.

Every sub-topic MUST have:
- Time estimate in italic
- 2-4 paragraphs of explanation with ML context
- At least one Python code block with `linenums="1"` for longer snippets
- At least one `!!! tip "Teaching Moment"` admonition
- One `!!! action "What to Do"` with `- [ ]` checkbox items (mix of watch/read/code with emoji icons)
- `**Resources:**` section with 3-5 annotated, free-first items with emoji type icons

Include exactly one `!!! tip "Why This Path"` admonition (place in Arrays sub-topic or as standalone -- cite Andrew Ng, fast.ai, NumPy as foundation for all ML libraries).

Use ```` ```python linenums="1" ```` for code blocks longer than 5 lines. Use ```` ```python ```` for short 1-3 line snippets. Show output in ```` ```text ```` blocks where meaningful.

Do NOT write API reference documentation. Teach the concept, show the pattern, link to official docs.
  </action>
  <verify>
    <automated>cd /home/a-khouja/ml_roadmap && python3 -c "
content = open('docs/python-ml/numpy.md').read()
checks = [
    ('H1 title', content.startswith('# NumPy')),
    ('prerequisite admonition', '!!! prerequisite' in content),
    ('total time', 'Total time' in content),
    ('learning outcomes', '## Learning Outcomes' in content),
    ('6 sub-topics', content.count('## ') >= 8),  # H2s: LO + 6 topics + takeaways
    ('teaching moments', content.count('Teaching Moment') >= 6),
    ('action items', content.count('!!! action') >= 6),
    ('checkboxes', content.count('- [ ]') >= 15),
    ('code blocks', content.count('python') >= 10),
    ('why this path', 'Why This Path' in content),
    ('key takeaways', '## Key Takeaways' in content),
    ('next up link', 'pandas.md' in content),
    ('resources sections', content.count('**Resources:**') >= 6),
]
for name, ok in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {name}')
failed = [name for name, ok in checks if not ok]
assert not failed, f'Failed: {failed}'
print('All checks passed')
"
    </automated>
  </verify>
  <done>NumPy page has 6 sub-topics with ML-motivated code examples, teaching moments, action checkboxes, curated resources, and follows the content template exactly</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Python ML section index page</name>
  <files>docs/python-ml/index.md</files>
  <action>
Replace the placeholder index.md with a full section index page matching the math-foundations/index.md pattern.

Structure:
1. H1: `# Python for ML`
2. `!!! prerequisite "Before You Start"` -- link to `../math-foundations/index.md` (Math Foundations), note basic Python knowledge assumed
3. Metadata: `*Total time: ~25-33 hours* | :green_circle: Beginner to :yellow_circle: Intermediate`
4. `## Why Python for ML?` -- 2-3 paragraphs on why these libraries form the essential toolkit. Mention that data preparation is 60-80% of ML work, and these tools are universal.
5. `!!! tip "Why This Path"` -- cite roadmap sources on Python ecosystem importance (Andrew Ng, fast.ai, Kaggle all assume these tools)
6. `## Section Overview` -- table with columns: Topic, Time, Difficulty, What You'll Learn. Four rows for NumPy, Pandas, Visualization, Scikit-learn. Use relative links (`numpy.md`, etc.) and difficulty emojis (`:green_circle:`, `:yellow_circle:`).
7. `## Recommended Order` -- brief paragraph explaining the linear path (NumPy -> Pandas -> Visualization -> Scikit-learn) and why.
8. `!!! info "Progress Tracking"` -- same localStorage note as math-foundations index.

Keep it concise (~35-45 lines). This is a navigation page, not a content page.
  </action>
  <verify>
    <automated>cd /home/a-khouja/ml_roadmap && python3 -c "
content = open('docs/python-ml/index.md').read()
checks = [
    ('H1 title', content.startswith('# Python for ML')),
    ('prerequisite', '!!! prerequisite' in content),
    ('total time', 'Total time' in content),
    ('why python', '## Why Python for ML?' in content),
    ('why this path', 'Why This Path' in content),
    ('section overview', '## Section Overview' in content),
    ('numpy link', 'numpy.md' in content),
    ('pandas link', 'pandas.md' in content),
    ('visualization link', 'visualization.md' in content),
    ('scikit-learn link', 'scikit-learn.md' in content),
    ('recommended order', '## Recommended Order' in content),
    ('progress tracking', 'Progress Tracking' in content),
    ('no placeholder', 'Placeholder' not in content),
]
for name, ok in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {name}')
failed = [name for name, ok in checks if not ok]
assert not failed, f'Failed: {failed}'
print('All checks passed')
"
    </automated>
  </verify>
  <done>Index page has prerequisite, topic overview table with all 4 pages linked, recommended order, Why This Path admonition, and no placeholder content</done>
</task>

</tasks>

<verification>
- All content checklist items (from .planning/CONTENT-CHECKLIST.md) satisfied for numpy.md
- Index page matches math-foundations/index.md pattern
- `mkdocs build` produces no errors for these pages
- Internal links resolve to existing files
</verification>

<success_criteria>
- NumPy page: 6 sub-topics, ML-motivated code examples, all checklist items checked
- Index page: full section overview matching math-foundations pattern
- Both files committed and building cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-python-ml-ecosystem-content/05-01-SUMMARY.md`
</output>
