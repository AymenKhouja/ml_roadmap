---
phase: 05-python-ml-ecosystem-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/python-ml/pandas.md
  - docs/python-ml/visualization.md
autonomous: true
requirements: [PYML-02, PYML-03]

must_haves:
  truths:
    - "Pandas page covers DataFrames, data loading, selecting/filtering, cleaning, grouping, and merging with realistic dataset examples"
    - "Visualization page covers Matplotlib fundamentals, customization, Seaborn, ML-specific plots, and EDA workflow"
    - "Every sub-topic has Python code examples with syntax highlighting and line numbers"
    - "Visualization page describes plot outputs in text and links to galleries rather than embedding images"
  artifacts:
    - path: "docs/python-ml/pandas.md"
      provides: "Complete Pandas content page with 6 sub-topics"
      min_lines: 180
    - path: "docs/python-ml/visualization.md"
      provides: "Complete Visualization content page with 5 sub-topics"
      min_lines: 150
  key_links:
    - from: "docs/python-ml/pandas.md"
      to: "docs/python-ml/visualization.md"
      via: "Next up link at page bottom"
      pattern: "\\*\\*Next up:\\*\\*.*visualization"
    - from: "docs/python-ml/visualization.md"
      to: "docs/python-ml/scikit-learn.md"
      via: "Next up link at page bottom"
      pattern: "\\*\\*Next up:\\*\\*.*scikit-learn"
---

<objective>
Write complete Pandas and Visualization content pages.

Purpose: PYML-02 requires Pandas coverage of data loading, cleaning, transformation, and EDA. PYML-03 requires visualization coverage of Matplotlib and Seaborn for ML analysis. These are both code-heavy pages at the beginner-intermediate level.
Output: Two files -- docs/python-ml/pandas.md and docs/python-ml/visualization.md
</objective>

<execution_context>
@/home/a-khouja/.claude/get-shit-done/workflows/execute-plan.md
@/home/a-khouja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTENT-CHECKLIST.md
@.planning/phases/05-python-ml-ecosystem-content/05-RESEARCH.md

Reference files to follow:
@docs/core-ml/supervised-learning.md (content template -- match this structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete Pandas content page</name>
  <files>docs/python-ml/pandas.md</files>
  <action>
Replace the placeholder pandas.md with a complete content page following the supervised-learning.md template. CODE-HEAVY page.

Structure (6 sub-topics from research):

1. **Page header:** H1 "Pandas", prerequisite admonition linking to `numpy.md` (NumPy section) since Pandas is built on NumPy arrays, metadata `*Total time: ~7-9 hours* | :green_circle: Beginner to :yellow_circle: Intermediate`, Learning Outcomes (4-5 bullets).

2. **Sub-topic 1: DataFrames and Series** (~1 hr) -- creating DataFrames, `.head()`, `.info()`, `.describe()`, `.dtypes`, `.shape`. ML connection: every tabular dataset is a DataFrame. Code: create a DataFrame from dict, inspect structure.

3. **Sub-topic 2: Data Loading and Export** (~45 min) -- `pd.read_csv()`, `pd.read_json()`, `.to_csv()`, handling common issues. ML connection: real datasets from files. Code: load a CSV (use sklearn's iris or describe loading a Kaggle CSV), show common parameters.

4. **Sub-topic 3: Selecting and Filtering Data** (~1 hr) -- `[]`, `.loc[]`, `.iloc[]`, boolean filtering, `.query()`. ML connection: feature selection, subsetting. Code: filter rows by condition, select specific columns.

5. **Sub-topic 4: Data Cleaning and Missing Values** (~1.5 hr) -- `.isna()`, `.fillna()`, `.dropna()`, `.duplicated()`, `.replace()`, type conversion. ML connection: data cleaning is 80% of ML work. Code: demonstrate cleaning workflow on messy data. Include the `!!! tip "Why This Path"` admonition here -- cite that data prep is 60-80% of ML practitioner time per Kaggle surveys and Andrew Ng.

6. **Sub-topic 5: Grouping, Aggregation, and Transformation** (~1.5 hr) -- `.groupby()`, `.agg()`, `.transform()`, `.pivot_table()`, `.value_counts()`. ML connection: feature engineering, summary statistics. Code: groupby with aggregation, value_counts for class distribution.

7. **Sub-topic 6: Merging, Joining, and Concatenation** (~1 hr) -- `pd.merge()`, `pd.concat()`, merge types (inner, outer, left, right). ML connection: combining multiple data sources. Code: merge two DataFrames on a key column.

8. **Page footer:** Key Takeaways (4-5 bold-phrase bullets), Next up link to `visualization.md`.

Same per-sub-topic requirements as NumPy page: time estimate, 2-4 paragraphs, code blocks with `linenums="1"`, Teaching Moment, action checkboxes with emoji icons, 3-5 curated resources with emoji type icons.

Use realistic examples. Do NOT just use toy `[1,2,3]` data -- use sklearn built-in datasets or describe realistic scenarios. Do NOT write API reference docs -- teach concepts and patterns.
  </action>
  <verify>
    <automated>cd /home/a-khouja/ml_roadmap && python3 -c "
content = open('docs/python-ml/pandas.md').read()
checks = [
    ('H1 title', content.startswith('# Pandas')),
    ('prerequisite', '!!! prerequisite' in content),
    ('total time', 'Total time' in content),
    ('learning outcomes', '## Learning Outcomes' in content),
    ('6 sub-topics', content.count('## ') >= 8),
    ('teaching moments', content.count('Teaching Moment') >= 6),
    ('action items', content.count('!!! action') >= 6),
    ('checkboxes', content.count('- [ ]') >= 15),
    ('code blocks', content.count('python') >= 10),
    ('why this path', 'Why This Path' in content),
    ('key takeaways', '## Key Takeaways' in content),
    ('next up link', 'visualization.md' in content),
    ('resources sections', content.count('**Resources:**') >= 6),
]
for name, ok in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {name}')
failed = [name for name, ok in checks if not ok]
assert not failed, f'Failed: {failed}'
print('All checks passed')
"
    </automated>
  </verify>
  <done>Pandas page has 6 sub-topics with realistic data examples, teaching moments, action checkboxes, curated resources, and follows the content template</done>
</task>

<task type="auto">
  <name>Task 2: Write complete Visualization content page</name>
  <files>docs/python-ml/visualization.md</files>
  <action>
Replace the placeholder visualization.md with a complete content page. CODE-HEAVY page with a special consideration: this is a visualization page on a static site, so describe plots in text and link to galleries rather than embedding images.

Structure (5 sub-topics from research):

1. **Page header:** H1 "Data Visualization", prerequisite admonition linking to `numpy.md` and `pandas.md`, metadata `*Total time: ~5-7 hours* | :green_circle: Beginner to :yellow_circle: Intermediate`, Learning Outcomes (4-5 bullets).

2. **Sub-topic 1: Matplotlib Fundamentals** (~1.5 hr) -- `plt.figure()`, `plt.subplots()`, `ax.plot()`, `ax.scatter()`, `ax.bar()`, labels, titles, legends. ML connection: the base plotting library. Code: create a scatter plot and line plot using the Figure/Axes pattern (NOT the pyplot state machine). Emphasize the object-oriented API.

3. **Sub-topic 2: Customizing Plots for Publication** (~45 min) -- colors, styles, annotations, `plt.style.use()`, figure sizing, `plt.savefig()`. ML connection: plots for reports and papers. Code: show style application and figure saving.

4. **Sub-topic 3: Seaborn for Statistical Visualization** (~1 hr) -- `sns.histplot()`, `sns.boxplot()`, `sns.violinplot()`, `sns.pairplot()`, `sns.heatmap()`. ML connection: higher-level statistical plots. Code: pairplot on Iris, heatmap of correlation matrix. Include the `!!! tip "Why This Path"` here -- cite Andrew Ng's "look at your data" principle and fast.ai's emphasis on EDA.

5. **Sub-topic 4: ML-Specific Visualizations** (~1.5 hr) -- confusion matrices, learning curves, feature importance bar charts, ROC curves, correlation heatmaps, decision boundaries. ML connection: these are the plots you need for every ML project. Code: show confusion matrix heatmap pattern, learning curve plot pattern. Describe what each plot shows visually since the static site cannot render them.

6. **Sub-topic 5: Exploratory Data Analysis (EDA) Workflow** (~1 hr) -- distribution plots, scatter plots with color coding, correlation matrices, identifying outliers. ML connection: the standard visual exploration process before modeling. Code: show a structured EDA sequence on a dataset.

7. **Page footer:** Key Takeaways (4-5 bold-phrase bullets), Next up link to `scikit-learn.md`.

IMPORTANT per research pitfall 6: Do NOT embed static plot images. Describe what each plot shows in text, provide code that generates it, and link to Matplotlib gallery and Seaborn gallery where learners can see visual examples. The action items should have learners generate the plots themselves.

Same per-sub-topic requirements: time, paragraphs, code, Teaching Moment, action checkboxes, resources.
  </action>
  <verify>
    <automated>cd /home/a-khouja/ml_roadmap && python3 -c "
content = open('docs/python-ml/visualization.md').read()
checks = [
    ('H1 title', content.startswith('# Data Visualization')),
    ('prerequisite', '!!! prerequisite' in content),
    ('total time', 'Total time' in content),
    ('learning outcomes', '## Learning Outcomes' in content),
    ('5 sub-topics', content.count('## ') >= 7),
    ('teaching moments', content.count('Teaching Moment') >= 5),
    ('action items', content.count('!!! action') >= 5),
    ('checkboxes', content.count('- [ ]') >= 12),
    ('code blocks', content.count('python') >= 8),
    ('why this path', 'Why This Path' in content),
    ('key takeaways', '## Key Takeaways' in content),
    ('next up link', 'scikit-learn.md' in content),
    ('resources sections', content.count('**Resources:**') >= 5),
    ('matplotlib ref', 'matplotlib' in content.lower()),
    ('seaborn ref', 'seaborn' in content.lower() or 'sns.' in content),
]
for name, ok in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {name}')
failed = [name for name, ok in checks if not ok]
assert not failed, f'Failed: {failed}'
print('All checks passed')
"
    </automated>
  </verify>
  <done>Visualization page has 5 sub-topics covering Matplotlib, Seaborn, and ML-specific plots with code examples, no embedded images, and links to galleries for visual reference</done>
</task>

</tasks>

<verification>
- All content checklist items satisfied for both pandas.md and visualization.md
- `mkdocs build` produces no errors for these pages
- Internal links resolve to existing files
- No embedded image files -- all visual references are external links or code the learner runs
</verification>

<success_criteria>
- Pandas page: 6 sub-topics with realistic data examples, all checklist items
- Visualization page: 5 sub-topics covering Matplotlib + Seaborn + ML plots, text descriptions of visuals
- Both files committed and building cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-python-ml-ecosystem-content/05-02-SUMMARY.md`
</output>
